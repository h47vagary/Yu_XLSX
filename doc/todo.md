# 待做

### 对"机动"车牌做匹配
#### 描述
在搜索条件中，"机动" 的车牌会带有加油卡号的后三位(机动xxx)。而数据源中，仅仅只有 "机动" 。导致精确匹配失败
优先级：低

#### 解决方法
在数据源中，存在"加油卡号"列，可以取该列的后三位填充后，即为 搜索条件中的 "机动xxx"

ps: 只有 “揭阳” 表有




### 搜索效率太低
#### 描述
On方 的搜索效率，表格一大就很慢了，待优化
优先级：高

#### 解决方法
- read_cell() 调用频繁，该接口应该调用到文件IO，可以先 read_all 到内存再进行操作
- 线程池并行搜索每个 sheet
- 对每个关键词都遍历整个搜索源表，可以建立哈希表索引或 unordered_map 缓存匹配
- 日期/字符串是后处理的，且每个循环处理一次，可以在读取时就处理
    - 日期：简化处理
    - 字符串：转小写处理
- 控制台输出减少
- 选择好搜索条件文件时，后台预加载打开

### UI 优化
#### 配置文件记忆路径
- 记忆上次到处路径
- 记忆搜索文件路径
- 记录数据源文件路径

优先级：中

#### 支持文件拖动放置
优先级：低

#### 美化界面
优先级：高

大概需求：

### 新增 “单价" "金额" 列
1. "单价" 根据 "数量"  值存在对应值
2. "金额" 值 为 "数量" x "单价" (舍去小数点）